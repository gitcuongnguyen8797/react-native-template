import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    btnContainer: {
        borderRadius: 10,
        padding: 10,
        minWidth: 100,
        minHeight: 50,
        // width:'100%'
    },
    btnText: {
        textAlign: "center",
        fontSize: 18,
        fontWeight: '900',
        color: 'white'
    }
})

export default styles